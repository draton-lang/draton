// RUN: compile
interface Drawable {
    fn draw()
    @type {
        draw: () -> Int
    }
}
class Circle implements Drawable {
    fn draw() { return 1 }
}
@type { render: (Drawable) -> Int }
fn render(d) { return d.draw() }
