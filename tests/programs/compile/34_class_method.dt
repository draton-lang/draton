// RUN: compile
class User {
    let name

    layer Access {
        fn getName() {
            return self.name
        }
    }

    @type {
        name: String
        getName: () -> String
    }
}

@type {
    main: (User) -> String
}

fn main(u) {
    return u.getName()
}
