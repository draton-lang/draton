// RUN: compile
class Shape {
    let color

    @type {
        color: String
    }
}
class Circle extends Shape {
    let radius

    @type {
        radius: Int
    }
}

@type {
    main: (Circle) -> String
}

fn main(c) {
    return c.color
}
