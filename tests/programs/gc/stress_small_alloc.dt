// GC stress: nhiều alloc nhỏ bằng fstring trong loop
fn main() -> Int {
    let mut i = 0
    let mut s = 0
    while (i < 10000) {
        let tmp = f"node {i}"
        s += i
        i++
    }
    s % 100
}
