# Self-Host AST

This directory contains the canonical Draton AST model used by the self-host compiler rewrite.

Current boundary:

- AST truth still comes from `crates/draton-ast`
- any self-host AST shape introduced here must remain semantically aligned with the Rust frontend
