# Hello Preview App

This example project is bundled with the Draton Early Tooling Preview.

Try it after installing `drat`:

```sh
cd examples/early-preview/hello-app
drat fmt --check src
drat lint src
drat task
drat task build
./build/hello-preview
```
